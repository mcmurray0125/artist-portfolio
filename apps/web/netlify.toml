[build]
  command = "pnpm install && pnpm --filter web build"
  publish = "apps/web/dist"
  functions = "netlify/functions"

[dev]
  functions = "netlify/functions"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
