[build]
  command = "pnpm install && pnpm --filter web build"
  publish = "apps/web/dist"
  functions = "apps/web/netlify/functions"

[dev]
  functions = "apps/web/netlify/functions"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
