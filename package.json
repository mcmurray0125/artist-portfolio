{
  "name": "artist-portfolio-content-operations",
  "private": true,
  "version": "1.0.0",
  "description": "Artist Portfolio Content Operations",
  "scripts": {
    "dev": "pnpm --filter studio exec npm run dev & pnpm --filter web exec netlify dev",
    "build": "pnpm run --parallel build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "@sanity/client": "^7.17.0"
  }
}